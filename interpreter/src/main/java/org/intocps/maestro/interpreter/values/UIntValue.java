package org.intocps.maestro.interpreter.values;

public class UIntValue extends IntegerValue {
    //TODO this is too small to contain a uint
    public UIntValue(int value) {
        super(value);
    }
}
