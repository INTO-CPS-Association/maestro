# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Maestro2

on: [workflow_dispatch, push, pull_request]
env:
  JAVA_VERSION: 11.0.3
  SERVER_ID: snapshots
  PYTHON_VERISON: '3.x'

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ windows-latest ]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Java
        uses: actions/setup-java@v1.4.3
        with:
          java-version: ${{ env.JAVA_VERSION }}
      - name: Build Version
        run: mvn help:evaluate -Dexpression=project.version -q -DforceStdout --file pom.xml
#      - name: Maven Build
#        run: mvn -B package --file pom.xml -DskipTests
#
#      - name: Setup Python
#        uses: actions/setup-python@v2
#        with:
#          setup-python: ${{ env.PYTHON_VERISON }}
#      - name: Install Python Dependencies
#        run: |
#          python -m pip install --upgrade pip
#          cd external_tester
#          pip install -r requirements.txt
#      - name: CLI Test
#        run: |
#          cd external_tester
#          python maestro_cli_test.py --path $pwd/../maestro/target/maestro-2.0.4-SNAPSHOT-jar-with-dependencies.jar