# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Maestro2

on: [workflow_dispatch, push, pull_request]
env:
  JAVA_VERSION: 11.0.3
  SERVER_ID: snapshots
  PYTHON_VERISON: '3.x'
  COE_VERSION: 2.0.4-SNAPSHOT

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
    steps:
      - name: Test
        run: echo ${{pwd}}/../maestro/target/maestro-${{env.COE_VERSION}}-jar-with-dependencies.jar
      - uses: actions/checkout@v2
      - name: Set up Java
        uses: actions/setup-java@v1.4.3
        with:
          java-version: ${{ env.JAVA_VERSION }}
#      - name: Maven Build
#        run: mvn -B package --file pom.xml -DskipTests
#
#      - name: Setup Python
#        uses: actions/setup-python@v2
#        with:
#          setup-python: ${{ env.PYTHON_VERISON }}
#      - name: Install Python Dependencies
#        run: |
#          python -m pip install --upgrade pip
#          cd external_tester
#          pip install -r requirements.txt
#      - name: CLI Test
#        run: |
#          cd external_tester
#          python maestro_cli_test.py --path $pwd/../maestro/target/maestro-${{env.COE_VERSION}}-jar-with-dependencies.jar
#      - name: Web API Test
#        run: |
#          cd external_tester
#          python maestro_cli_test.py --path $pwd/../maestro-webapi/target/maestro-webapi-${{env.COE_VERSION}}.jar