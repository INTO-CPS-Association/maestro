module DerivativeEstimator
import DerivativeEstimatorInstance
{
    DerivativeEstimatorInstance create(uint indexesOfInterest[], uint order[], uint provided[])
}

module DerivativeEstimatorInstance
{
   boolean estimate( real time, real sharedData[], real sharedDataDerivatives[][])
   boolean rollback( real sharedDataDerivatives[][])
}